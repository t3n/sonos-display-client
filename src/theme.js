const COLOR_PRIMARY = '#ffffff';
const COLOR_SECONDARY = '#1d1d1d';
const COLOR_BACKGROUND = '#f9423a';

const colors = {
  primary: COLOR_PRIMARY,
  secondary: COLOR_SECONDARY,
  background: COLOR_BACKGROUND
};

const theme = { colors };

export default theme;
